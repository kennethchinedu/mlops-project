# Progress

**Goal:** one end-to-end automated MLOps project (see CURRICULUM.md) + entry-level MLOps engineer readiness
**Current milestone:** M2 — Dataset, EDA & a real cleaning pipeline (capstone review milestone)
**Previous milestone:** M1 CAPSTONE — CLI dataset profiler — CORE FUNCTIONALITY PASSED 2026-07-26 (see lessons/m1-capstone.md). One optional follow-up not fully closed: testing against a CSV with genuinely different column names (logged, non-blocking). Student chose to move on to M2 rather than close that gap first.
**Project dataset:** `~/Desktop/Mlops/Datasets/Hotel_Reservation.csv` — chosen 2026-07-26. ~36,280 rows, 18 columns, binary prediction target `booking_status` (Canceled / Not_Canceled). Real messiness confirmed present (at least one `NaN` visible in `market_segment_type` on inspection). Note: a second file in the same folder, `Hotel_Reservations_Data.csv`, was checked and rejected — despite its `.csv` extension it's actually a raw `.xlsx`/ZIP file, not real CSV data; do not use it.
**Current lesson:** 2.5 — logging + second cleaning function: drop rare Meal Plan 3 (assigned 2026-07-27)
**Dataset EDA findings so far (from lesson 2.2, will drive cleaning-pipeline decisions):**
- Target `booking_status` imbalanced ~33% Canceled / 67% Not_Canceled — real skew, not extreme; must be considered in M3 (a naive always-predict-majority model gets ~67% accuracy "for free")
- `type_of_meal_plan` has a near-empty category `Meal Plan 3` (only 5 of 36,278 rows) — decision made in lesson 2.4: drop those rows (too small a fraction to be meaningful) — NOT YET IMPLEMENTED as its own cleaning function
- `type_of_meal_plan`'s disguised missing value `"Not Selected"` (5,130 rows) — FIXED in lesson 2.4 via `clean_meal_plan` in `src/cleaning.py`
- `.value_counts()` hides NaN by default (verified: `market_segment_type` sums to 36,261 without `NaN`, needs `dropna=False` to show the 18 actual missing as their own row) — not yet cleaned (still just documented, no function written for `market_segment_type`, `arrival_year`, or `avg_price_per_room`'s missing values yet)

## Daily targets — 2026-07-27
- [ ] Lesson 2.5: plain-English function + logging plan reviewed before coding
- [ ] REPL lab: logging levels (DEBUG/INFO/WARNING), what prints vs. silent, recorded in notes.md
- [ ] Test-first: drop_rare_meal_plan (type hint), tests pass
- [ ] Function logs an info/warning message with the real dropped-row count
- [ ] Verified against real dataset: log reports exactly 5 rows dropped
- [ ] Check-yourself answered; committed + pushed
- [ ] Check-yourself answered; committed + pushed

## Completed lessons
- 0.1 — Python verification + git basics (PASSED 2026-07-10 after 3 review rounds)
- 0.2 — Virtual environments (PASSED 2026-07-10; needed several rounds on PATH/activation mechanism but demonstrated it in the end, incl. correct two-terminal prediction)
- 0.3 — pip freeze & requirements.txt (PASSED 2026-07-11; exercise flawless, but needed full walkthrough on freeze-vs-install-r direction; re-verified by rebuilding env from memory)
- 0.4 — GitHub remote & push (PASSED 2026-07-11; repo live at github.com/kennethchinedu/mlops-project, tracking set up, proved commit→push loop; initially left answers uncommitted — good lesson in "unpushed = at risk")
- 1.1 — Variables, types, strings, f-strings, input() (PASSED 2026-07-11 after 4 review rounds; recurring issues: skimming feedback instead of reading it — 3 items re-flagged; pasted docs example code verbatim; confused `//` with `%` repeatedly before clearing it)
- 1.2 — Lists, dicts, loops: mini data-quality profiler (PASSED 2026-07-16 after 6 review rounds; bugs hit in order: `=+1` typo instead of `+=`, inverted if/else logic, `elif` chaining independent checks (masked missing values when multiple fields were None in the same row), crash calling `.items()` on a list, leftover unlabeled prints alongside the report, stray `input()` left over from lesson 1.1 habits. Good: self-corrected `==` to `is None` once, built a harder test dataset with overlapping missing fields unprompted.)
- 1.3 — Functions: split profiler into `dataset_summary` (counts, returns 4 values as a tuple) + `data_info` (prints report) (PASSED 2026-07-23 after ~9 review rounds; bugs hit in order: indexed a string key (`key["name"]`) instead of comparing `key`/`value` directly, inverted check (`if key is None` instead of `if value is None`), `UnboundLocalError` from `missing_name`/`row_count` declared outside the function while mutated inside — first real collision with function scope, swapped which function's call needed unpacking vs. which needed arguments passed in, row count incremented once per field instead of once per row (loop-nesting mistake, same class of bug as lesson 1.2). Good: correctly used `elif` for the three key checks since they're genuinely mutually exclusive (contrast with the 1.2 `elif` bug); once shown the tuple-unpacking mechanism via a tiny isolated REPL example, applied it correctly to the real 4-value case unprompted. Also hit a GitHub push 403 — stale `hexanams` credential cached in macOS osxkeychain; fixed by clearing the Keychain entry and re-authenticating as `kennethchinedu`.)
- 1.4 — Exceptions & tracebacks: `dataset_summary` now catches malformed rows (missing key → `KeyError`, non-dict row → `TypeError`) without crashing, reports a `bad_rows` count (PASSED 2026-07-23, same-day as 1.3, after ~7 review rounds; bugs hit in order: manually reimplemented `KeyError` with `if "name" not in data: raise KeyError(...)` instead of letting a direct lookup raise it naturally, then a worse version — `if "name":` — which is always truthy and fired on every row including clean ones, bare `except: pass` right after having just proven in the REPL lab why that's dangerous, `except` block referenced an undefined `key` variable left over from a deleted `.items()` loop (would have raised `NameError` if triggered), guessed `except ValueError` for the non-dict case instead of verifying — correctly self-corrected to `TypeError` once told to test `"hello"["name"]` directly, accidentally mutated the lesson-1.3 `new_data` dataset instead of building a separate third one (had to be restored), `bad_rows += 1` placed inside `try` (so it counted successful rows as bad) before correctly moving it into both `except` blocks. Good: correctly identified real exception types via REPL evidence rather than guessing on the two genuine malformed-data cases (`KeyError` for missing key, `AttributeError` for non-dict via `.items()`, later correctly revised to `TypeError` once the code switched to direct indexing); independently ruled out "non-string dict keys" as a real exception case after testing `5 == "name"` and seeing it just returns `False`; restructured from `.items()`-based checks to direct-lookup checks correctly on first attempt once the mechanism was explained.)
- 1.5 — Reading files: `read_data(datapath)` reads a real CSV via `csv.DictReader`, converts empty-string cells to `None`, handles a missing file by returning `[]` instead of crashing; wired unchanged into lesson 1.4's `dataset_summary`/`data_info` (PASSED 2026-07-25, spanned 2026-07-23 to 2026-07-25 with a gym-break mid-session, ~15+ review rounds; bugs hit in order: reassigning the loop variable `value = None` instead of `row[key] = None` (didn't persist), `.append(row)` indented inside the inner per-field loop instead of the outer per-row loop (tripled every row into the dataset), a ragged/malformed test CSV (row with fewer columns than the header) that silently shifted every column over — self-corrected to a proper empty-cell test case once flagged, relative-path confusion running the script from different working directories (`practice/data/...` vs `data/...` vs a `pratice` typo) causing repeated false "file not found", `except FileNotFoundError` block had no `return` at all (defaulted to `None`, crashed `dataset_summary` with `'NoneType' object is not iterable`) — fixed by returning `[]`, then downstream code still called `dataset_summary`/`data_info` unconditionally even on a failed read (misleading "0 rows" output) until guarded with `if data == []:`, called `read_data` twice with two different (one typo'd) paths in the if/else branches instead of once — corrected to call once and reuse the variable, second test dataset initially replaced the first instead of adding a new one — corrected via a `check_data(datapath)` function to run both without duplicating the report logic. Good: independently identified and explained the `import`-triggers-top-level-code side effect (lesson3.py's demo calls leaking into lesson5.py's output) and applied `if __name__ == "__main__":` to both files unprompted after the mechanism was explained; correctly reasoned through the empty-string-vs-`None` distinction and tied it back to why the conversion step was needed for `is None` checks to work on CSV data; added `__pycache__` to `.gitignore` after being walked through why compiled bytecode shouldn't be committed (paralleling the existing `.venv` reasoning).)
- 1.6 — Modules & project layout: split reusable logic into `practice/profiler.py` (pure module: `dataset_summary`, `data_info`, `read_data`, `check_data`, zero top-level calls) + `practice/lesson6.py` (thin entry point, everything under `if __name__ == "__main__":`) (PASSED 2026-07-25, same-day as 1.5, review moved quickly — most of the mechanism was already understood from lesson 1.5's `__name__` fix; bugs hit in order: named a function `print`, shadowing the builtin and causing infinite self-recursion inside it — self-corrected once asked to test it directly; wrote `import function1` instead of `from file1 import function1` (confusing a function name with a module/file name); in the scratch experiment, indented a function's `def` inside the `if __name__ == "__main__":` guard alongside the code meant to be conditional, so the function never got defined on import (`ImportError`) — same "which lines need to always exist vs. which are the actual experiment" confusion showed up twice before separating them correctly; briefly mis-stated that a pure module producing no output when run directly was "a problem" before correctly revising to "expected, because that's what the file is for." Good: independently ran the 2-file import experiment and correctly reasoned out loud about `__name__`'s value during import vs. direct execution before being told; deleted the untracked scratch `tiny-folders/` unprompted as a reasonable cleanup call; made a deliberate, well-reasoned choice to keep `lesson5.py` as a historical/learning artifact rather than deleting it, while understanding `profiler.py` is now the single source of truth going forward.)
- 1.7 — CLI args & dynamic columns: NOT FORMALLY COMPLETED — student chose to skip ahead to the M1 capstone partway through after ~5-6hrs across two sessions stuck on one sub-problem (absent-key detection). Both of 1.7's real goals (sys.argv, dynamic dict-based column counting) were fully achieved anyway, just delivered as part of the capstone below instead of as a standalone lesson artifact. See M1 CAPSTONE entry for the actual final code and verification.
- **M1 CAPSTONE — CLI Dataset Profiler** (core functionality PASSED 2026-07-26, spanning 2026-07-25 to 2026-07-26; see lessons/m1-capstone.md for full requirements). Final structure: `practice/profiler.py`'s `dataset_summary` does two-pass dynamic column detection — first pass collects the union of every key seen across all rows into a `columns` list (no hardcoded names, `isinstance` check skips non-dict rows during collection), second pass checks each row against every known column via `if column not in row or row[column] is None: counts[column] += 1` — correctly catching BOTH a present-but-`None` value AND a column entirely absent from a given row (the exact gap left open from lesson 1.7). `data_info` prints a real "No missing values found" message when `counts` is empty, instead of silent output. `practice/lesson6.py` is the CLI entry point: real `sys.argv[1]` for the file path (no hardcoded path anywhere), `except IndexError` for a missing argument with a clear usage message, all under `if __name__ == "__main__":`. Verified against 4 real cases end-to-end through the actual `lesson6.py` → `profiler.py` chain: `data.csv` (1 missing name, 1 missing school, correctly reported), `data2.csv` (fully clean, correctly prints the "no missing values" message instead of nothing), no CLI argument (clean usage message, no traceback), nonexistent file path (clean "file not found" message, no crash). Bugs hit in order across the two-session arc: reused the builtin name `dict.get` literally instead of `counts.get` (confused the type with the actual variable) — this was itself the root cause of a second, seemingly-unrelated `TypeError`/`AttributeError` cascade that disappeared once fixed; `for key, value in counts:` missing `.items()`, causing `ValueError: too many values to unpack`; f-string `{key:value}` misread as a format-spec instead of two separate substitutions; **the big one** — calling `dataset_summary` on 3 different datasets while reusing the exact same result-variable names across all 3 calls, silently overwriting earlier results (same root category of bug as the lesson 1.4 dataset-naming collision, recurred despite having been explicitly cleared once before); a fix attempt using a hardcoded `expected_columns` + exact-`set()`-match that regressed on two fronts (reintroduced hardcoding; conflated "missing one field" with "not a dict at all") — correctly self-identified as a regression when asked to trace it, and abandoned. Good: independently derived and implemented the full two-pass dynamic-column algorithm from a plain-English description with no code shown — this is a genuine blank-page-fluency data point per [[end-goal-blank-page-fluency]]; correctly reasoned through *why* the absent-key problem can't actually occur with real CSV input (since `csv.DictReader` guarantees every row shares the same header-derived keys) while still choosing, with real justification, to keep the more defensive general-purpose version rather than simplify; used `isinstance(row, dict)` unprompted as a cleaner replacement for the old `try`/`except TypeError` scaffolding. Acknowledged open gap, not blocking: not yet tested against a CSV with genuinely different column names (only `data.csv`/`data2.csv`, which share the same 3 columns) — the "works on any schema" claim is verified by code inspection, not by an actual differently-shaped test file.

## Weak spots to revisit
- Commit message quality — STILL OPEN: 2026-07-11 commits were diary-style ("learnt about datatypes...") instead of describing the snapshot; next diary-style message gets rejected
- Answering "why" questions with mechanisms, not restated facts
- Reading review feedback fully before fixing — 3 items had to be re-flagged in lesson 1.1
- `//` (floor division) vs `%` (remainder) — cleared verbally 2026-07-11, re-quiz later to confirm
- `elif` vs independent `if`s — confused these when checking multiple unrelated conditions on the same row (lesson 1.2 round 3); understood after being asked to trace a row with two missing fields through the chain
- `=+` vs `+=` typo — caught in lesson 1.2 round 2 review, not yet re-quizzed
- `== None` vs `is None` — flagged twice in lesson 1.2 (reverted once after fixing), self-corrected on final round; re-quiz to confirm it's stuck
- Leaves stray/leftover code from earlier drafts (dead prints, unused input()) instead of cleaning before submitting for review — recurring across lesson 1.2 rounds 3-5; NOTE: cleaned up unprompted before final submission in lesson 1.3, first time doing so without being told
- Function scope (reading a variable declared outside a function is fine, but `+=`/reassigning it inside without passing it in triggers `UnboundLocalError`) — hit live in lesson 1.3 round 4-5; re-quiz to confirm it's stuck
- Mixing up which function's call needs tuple-unpacking (`a, b = f()`) vs. which needs values passed as arguments (`g(a, b)`) — confused in lesson 1.3; cleared after being walked through a minimal 2-value REPL example separate from the real code
- Reimplementing exceptions manually (`if x not in data: raise KeyError`) instead of trusting the natural operation to raise — took two wrong attempts in lesson 1.4 (a working-but-redundant version, then a broken `if "name":` that's always truthy); cleared once shown that a direct lookup raises on its own
- `try`/`except` block placement — confused which zone (`try` body vs. `except` body) represents success vs. failure; placed a `bad_rows` counter inside `try` (counting successes as failures) before correctly moving it into `except` — lesson 1.4 round 6-7, needed a from-scratch conceptual explanation, not just a hint
- Bare `except:` — used it once in lesson 1.4 immediately after proving in the REPL lab why it's dangerous; self-caught the contradiction when asked to compare against own notes.md finding
- Relative file paths depend on the current working directory, not the script's location — caused repeated false "file not found" errors across lesson 1.5 by switching between running from project root and from `practice/`; understood the mechanism but hasn't yet learned the `__file__`-based fix (flagged as a future topic, not required yet)
- Loop-variable reassignment vs. mutating the container (`value = None` vs `row[key] = None`) — same root confusion as scope issues in lesson 1.3/1.4, resurfaced in lesson 1.5's CSV-cleaning step; corrected on first hint this time
- Placing `.append()`/counters at the wrong loop-nesting level (inner vs. outer loop) — third occurrence of this exact class of bug (lesson 1.2 elif-chaining, lesson 1.3 row_count, lesson 1.5 dataset.append) — worth a dedicated re-quiz on "what does this loop run once per" before the M1 capstone
- Shadowing builtins by reusing their name (e.g. `def print():`) — one-off in lesson 1.6, self-corrected quickly once asked to test the recursion directly; watch for recurrence with other common builtin names (`list`, `dict`, `id`, `type`, `str`)
- Confusing a function name with the module/file name it lives in when writing `import` statements (`import function1` instead of `from file1 import function1`) — one-off in lesson 1.6, corrected on first hint by re-deriving from the working lesson 1.5 example
- Reusing the same result-variable names across multiple calls to the same function, silently overwriting earlier results — recurred in the M1 capstone work (2026-07-26) despite being explicitly cleared once already in lesson 1.4; this is now a confirmed *recurring* pattern, not a one-off — worth an explicit re-quiz ("why do these two calls need different variable names?") early in M2
- Shadowing a variable/method name with the builtin/type it's meant to call (`dict.get(...)` instead of `counts.get(...)`) — same family as lesson 1.6's `def print():` shadowing; caused a confusing cascade of unrelated-looking errors downstream before being traced back to this one root cause
- Placing a bare/direct function call (or unguarded CSV read) at the top level of a file meant to be imported by another tool — now a confirmed *recurring* pattern across FOUR distinct contexts: lesson 1.5/1.6 (module import running demo code), lesson 2.3 (a bare `test_func()` call crashing pytest's collection step), lesson 2.4 (`src/cleaning.py` initially had a top-level `pd.read_csv(...)` + function call before the real dataset was even needed). Same root cause every time (top-level code executes on import, not just on direct run) but the *symptom* looks different per context, so it keeps getting re-diagnosed from scratch rather than recognized instantly — worth explicitly naming the general rule up front in a future lesson ("don't put executable calls at a file's top level unless guarded by `__name__ == \"__main__\"`") as a standing checklist item, since 4 recurrences means the Socratic re-derivation isn't sticking on its own
- Predicting behavior instead of testing it in the REPL first — hit in lesson 2.3 (assumed `double("hello")` would error, was wrong — `"hello" * 2` actually returns `'hellohello'`) — GOOD sign: self-corrected by actually checking in the REPL rather than defending the wrong guess, worth reinforcing as the right instinct
- `is` vs `pd.isna()` for missing-value checks in pandas — wrote `assert result[2] is pd.NA` in lesson 2.4, which passed but for the wrong reason (checks object identity, not missing-ness); once shown a direct REPL counterexample (`float("nan") is pd.NA` → `False`), correctly reasoned that this would silently fail on the real dataset's actual `NaN` representation and fixed it unprompted — good recovery, but note pandas has at least three different "missing" representations (`None`, `float("nan")`, `pd.NA`) that this exact confusion could resurface around
- Environment quirk, not a knowledge gap: bare `pytest <path>` vs `python3 -m pytest <path>` gave different results (the latter adds the current directory to Python's import path, the former doesn't reliably) — causing a confusing `ModuleNotFoundError: No module named 'src'` that had nothing to do with the code. Standardize on `python3 -m pytest` for this project going forward.

## Cleared weak spots
- 2026-07-11 M0 quiz (5/5): staging vs tracked ✓, PATH/venv activation ✓, freeze vs install -r ✓ — all answered cold

## Review notes
- 2026-07-10 — Lesson 0.1 review #1: **NOT YET.** Python 3.13.7 ✓, git init ✓, config ✓. Missing: notes.md (main deliverable, answers were written into the assignment sheet instead); commit message was the instruction text pasted verbatim; used `git add .` instead of selective staging; Q1/Q3 check answers didn't answer the question. Positive: created .gitignore unprompted to exclude teacher files.
- 2026-07-16 — Lesson 1.2 review, 6 rounds, final verdict **PASS**. Final script: 5-row list-of-dicts dataset, independent `if age/city/name is None` checks (not `elif`), correct counters for rows + missing-per-key, single f-string report, no `input()`. Still owed before lesson 1.3: answer the two "check yourself" questions in notes.md (dict-vs-.items() looping; empty-list edge case), then commit + push with a non-diary message.
- 2026-07-23 — Lesson 1.3 review, ~9 rounds, final verdict **PASS**. Final script: `dataset_summary(dataset)` returns `(missing_city, missing_name, missing_school, row_count)` as a tuple; `data_info(...)` takes those 4 values and prints the report; both functions run correctly against two differently-shaped datasets (2-row and 5-row) with verified-by-hand counts. All 3 check-yourself questions answered correctly in notes.md (parameter vs. argument initially swapped, corrected on request). Also resolved a git push 403 (stale `hexanams` credential in macOS Keychain) by clearing the osxkeychain entry and re-authenticating as `kennethchinedu` — both commits pushed and confirmed against `origin/main`.
- 2026-07-23 — Lesson 1.4 review, ~7 rounds, final verdict **PASS**. Final script: `dataset_summary` uses direct key lookups (`data["name"]`, etc.) inside `try`, catches `KeyError` (missing key) and `TypeError` (non-dict row, e.g. a string or int in the dataset) separately with real reporting (not bare `except`/not `pass`), tracks a `bad_rows` counter incremented only in the `except` blocks. Verified against 3 datasets: 2 clean (`data`, `new_data` — 0 bad rows each, correct missing-value counts) + 1 fully malformed (`malformed_data`, 4/4 rows bad, all caught cleanly, no crash). All 3 check-yourself questions answered correctly in notes.md (raised vs. handled; why specific exception types over bare `except:`, with concrete examples like `KeyboardInterrupt`; what `finally` does — sourced from docs, not a REPL run, which was fine since the checklist's REPL requirement was scoped to `KeyError`/`except` only). Committed as `bc88f6d`, pushed, confirmed clean against `origin/main`.
- 2026-07-25 — Lesson 1.5 review, spanned 3 days with a gym-break mid-session, ~15+ rounds, final verdict **PASS**. Final script: `read_data(datapath)` in `practice/lesson5.py` opens a CSV with `csv.DictReader` inside `with`, converts `''` cells to `None`, catches `FileNotFoundError` and returns `[]` on failure; a `check_data(datapath)` helper guards against summarizing an empty/failed read and calls `dataset_summary`/`data_info` from lesson3.py unchanged. Verified against 2 real CSV files on disk (`practice/data/data.csv` — 2 genuine missing values, `data2.csv` — fully clean), both producing correct reports; a missing-path test correctly prints a clean message and skips the summary instead of crashing. Both `lesson3.py` and `lesson5.py` guarded with `if __name__ == "__main__":`, added unprompted once the import-side-effect problem was explained (lesson3's demo calls were leaking into lesson5's output on `import`). `.gitignore` updated to exclude `__pycache__`. All 3 check-yourself questions answered correctly in notes.md. Commits `b7e3094`, `1bd9ef0`, `f41ee7d` pushed, confirmed clean against `origin/main`.
- 2026-07-25 — Lesson 1.6 review, same-day as 1.5, moved quickly, final verdict **PASS**. Final structure: `practice/profiler.py` holds `dataset_summary`/`data_info`/`read_data`/`check_data` as pure `def`s with zero top-level calls (verified: running it directly and importing it both produce zero output/side effects); `practice/lesson6.py` imports from it and runs the full pipeline on both CSV files entirely inside `if __name__ == "__main__":`. Output byte-for-byte identical to lesson 1.5's. All 3 check-yourself questions answered correctly in notes.md after one revision (initially mis-judged a silent pure-module run as "a problem" before correctly reframing it as expected behavior). Scratch `tiny-folders/` experiment (never tracked in git) deleted after use; `lesson5.py` deliberately kept as a historical artifact, not superseded in-place. Committed as `ae8dc09`, pushed, confirmed clean against `origin/main`.
- 2026-07-26 — M1 Capstone review (CLI dataset profiler), spanned 2 days, final verdict **CORE PASS**. Ran the actual `python3 practice/lesson6.py <path>` command against 4 real cases and verified each by hand against the source CSVs: `data.csv` → correct (`name:1`, `school:1`, 0 bad rows, 4 total); `data2.csv` → correct ("No missing values found" message, not silence); no argument → clean usage message, no traceback; nonexistent path → clean "file not found" message, no crash. Dynamic two-pass column detection in `dataset_summary` (practice/profiler.py) independently designed and implemented by the student from a plain-English description, no code shown — correctly catches both a present-`None` value and a column entirely absent from a row. Not yet done: a test file with genuinely different column names (only same-schema CSVs tested so far) — logged as an acknowledged, non-blocking gap. Full bug/good-move detail logged under the M1 CAPSTONE entry in Completed Lessons above.
- 2026-07-26 — Lesson 2.1 review (pandas basics on the real dataset), 1 round, final verdict **PASS**. `practice/lesson_pandas.py` loads the real `Hotel_Reservation.csv` (referenced from its shared location outside the repo, not copied in) with `pd.read_csv`, prints `.info()` (columns/dtypes/non-null counts), `.isnull().sum()` (missing-per-column), and `.shape` — all pandas built-ins, zero manual loops. Cross-verified by hand: `market_segment_type`'s 18 missing values matched `36279 total - 36261 non-null` computed from `.info()`'s own output, confirming `.isnull().sum()` agrees with `.info()`. `requirements.txt` correctly updated with both `pandas==2.2.3` and `numpy==2.5.1` (numpy came in as pandas' own dependency, not installed directly — student initially mis-copied numpy's version as `2.2.3`, matching pandas', then corrected it to the real installed version after being asked to check `pip freeze`). All 3 check-yourself questions answered correctly in notes.md. Committed as `fa7017a`, pushed, confirmed clean against `origin/main`.
- 2026-07-26 — Lesson 2.2 review (real EDA on the project dataset), same-day as 2.1, final verdict **PASS**. `practice/lesson_eda.py` uses `.value_counts()` and `.describe()` (no manual loops) to surface real dataset-quality findings, all verified with actual output, not guesses: target `booking_status` imbalance quantified at ~33/67 with a correct model-bias impact statement; `type_of_meal_plan`'s `Meal Plan 3` flagged as statistically unusable (5 of 36,278 rows); `type_of_meal_plan`'s `"Not Selected"` (5,130 rows) correctly identified as a disguised missing value that `.isnull().sum()` alone would never surface — a genuinely sharp catch, not prompted directly; `.value_counts()`'s default NaN-hiding behavior directly demonstrated with real side-by-side output (`market_segment_type` with/without `dropna=False`, the 18-row NaN count appearing only in the second version). One minor early bug: `print(df["market_segment_type"]).value_counts()` — mis-parenthesized, chaining `.value_counts()` onto `print()`'s return value (`None`) instead of onto the column — self-corrected after being walked through what `print()` actually returns (direct callback to lesson 1.3's check-yourself question). Committed as `cd89328`, pushed, confirmed clean against `origin/main`.
- 2026-07-26 — Lesson 2.3 review (pytest basics), same-day as 2.1/2.2, final verdict **PASS**. `practice/lesson_pytest.py`: `double(n)` returns `n * 2`, with `test_double_with_normal_number` (`double(2) == 4`) and `test_double_with_string` (`double("hello") == "hellohello"`, testing Python's string-repeat behavior for `*`, not arithmetic) — both correctly named and passing. Student personally witnessed a full break/fix cycle: changed `double` to `n * 3`, reran pytest, got real diagnostic output (`assert 6 == 4`, plus a character-level diff on the string case: `- hellohello` / `+ hellohellohello`), then fixed it back and confirmed both green again. Bugs hit in order: first version used `input()` inside `double()` itself, making it untestable (a test can't control keyboard input) — corrected to a proper `double(n)` parameter once asked to recall lesson 1.3's point of parameters; a bare `test_double_with_string()` call left at the top level of the file caused a pytest "error during collection" (same top-level-code-runs-on-import class of bug as lessons 1.5/1.6, applied to a new context: pytest imports the file to find tests, so a bare call fires during that import, not during actual test running) — corrected by removing it and trusting pytest's own auto-discovery; initially predicted `double("hello")` would error, correctly investigated in the REPL instead of assuming and found `"hello" * 2` actually returns `'hellohello'` — updated the edge-case test to match real behavior rather than the original guess. All 3 check-yourself questions answered correctly in notes.md (one round of revision: "htest" typo when explaining pytest's `test_` prefix auto-discovery). `pytest==8.3.4` correctly present in requirements.txt. Committed as `173be3a`/`a8278aa`, pushed, confirmed clean against `origin/main`.
- 2026-07-27 — Lesson 2.4 review (first real cleaning function, test-first + type hints), final verdict **PASS**. First lesson writing actual pipeline code outside `practice/`: `src/cleaning.py` holds `clean_meal_plan(data: pd.Series) -> pd.Series`, converting the disguised-missing `"Not Selected"` string to real `np.nan` via `.replace()`, guarded with `if __name__ == "__main__":` for the real-data verification block. `tests/test_cleaning.py` has 3 tests, written genuinely test-first (the first test was run and confirmed failing with `NameError: name 'clean_meal_plan' is not defined` before the function existed) — normal-value-untouched, disguised-value-becomes-NaN, and already-NaN-stays-NaN, all using `pd.isna()` correctly (not `==`/`is`) after a real, concrete misstep: initially wrote `assert result[2] is pd.NA`, which happened to pass but for the wrong reason (object identity, not missing-ness) — when walked through a live REPL comparison (`float("nan") is pd.NA` → `False`, but `pd.isna()` on both → `True`), correctly articulated in his own words that `is` would fail to detect missingness on the *real* dataset's `NaN` representation even though it passed on this specific test's hand-picked `pd.NA` sample, and fixed it unprompted. Verified against the real dataset with actual before/after `.value_counts(dropna=False)`: `"Not Selected"` (5,130) → 0, `NaN` 1 → 5,131 — arithmetic checks out exactly (1 + 5,130 = 5,131). Other bugs hit in order: `src/cleaning.py` initially had top-level CSV-reading + a bare function call (same recurring top-level-code-on-import pattern as 1.5/1.6/2.3) — corrected by moving to `__main__` guard; import path errors in the test file (`from src import clean_meal_plan` missing the actual filename, then a stray space `from src .cleaning import ...` that happened to still work); ran bare `pytest` instead of `python3 -m pytest` and got a different, confusing `ModuleNotFoundError` — resolved by standardizing on `python3 -m pytest` for this project; a leading-space typo in the string being replaced (`" Not Selected"` vs `"Not Selected"`) caught before it caused a silent failure; forgot to capture `clean_meal_plan`'s return value in the real-data verification script, producing identical before/after output that looked like nothing had changed — correctly diagnosed once asked to reason about mutation vs. return. `Meal Plan 3` (5 rows) correctly left out of scope for this specific function, with a reasoned decision recorded (drop, given how small a fraction it is) for a future function to implement. All 3 check-yourself questions answered correctly in notes.md. Committed as `00f20ec`/`9fefb30`, pushed, confirmed clean against `origin/main`.
